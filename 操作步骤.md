# 首次发布操作步骤

从零开始，大约 10 分钟。做完一次之后，以后更新只要双击 `发布更新.bat`。

---

## 第 1 步：注册 / 登录 GitHub

打开 <https://github.com> ，没有账号先注册（用邮箱注册即可，免费账号足够）。

## 第 2 步：创建仓库

1. 登录后点右上角 **`+`** → **New repository**
2. 按下表填写：

| 字段 | 填什么 |
| --- | --- |
| Repository name | 建议 `gd-safety-quality-demo`（只能用英文、数字、`-`、`_`） |
| Description | 选填，例如 `安全质量管理模块演示原型` |
| 可见性 | 选 **Public**（免费账号的 Pages 只支持公开仓库） |
| Add a README file | **不要勾** |
| Add .gitignore | **不要选** |
| Choose a license | **不要选** |

3. 点 **Create repository**

创建完成后浏览器地址栏是 `https://github.com/<你的账号>/gd-safety-quality-demo`，请记住 **账号名** 和 **仓库名**，下一步要用。

> ⚠️ 这一步只需在网页点，不用在本地做任何事。仓库里现在是空的，这是正常的。

> 🔴 **顺序很重要**：GitHub Pages 的 Branch 下拉只列出**仓库里已经存在的分支**。刚建好的空仓库一个分支都没有，所以那时下拉是空的、选不了 `main`。
> 正确顺序是：**先推送（第 5 步）→ 再开 Pages（第 6 步）**。不要在这里就去开 Pages。

## 第 3 步：推送凭证（两条路，选一条）

推送代码不能用账号密码。**推荐走 A，可以完全跳过 GitHub 的令牌页面。**

### 路 A：浏览器登录（推荐，不用建令牌）

你机器上已经装了 Git Credential Manager（git 自带的登录助手），直接用它：

1. 正常双击 `首次配置.bat`，**唯独在问令牌那一步直接按回车跳过**
2. 双击 `发布更新.bat` 推送时，会弹出一个登录窗口
3. 在窗口里选 **Browser**（浏览器）→ 打开的网页里用 GitHub 账号点 **Authorize** 授权
4. 授权成功后凭证自动保存，以后发布都不用再登录

> 这也是 GitHub 官方推荐的做法——官方文档明确写着命令行可以用 Git Credential Manager 代替创建令牌。

### 路 B：创建令牌 PAT

如果路 A 的窗口没弹出来、或你想用令牌，走这条。

> 🔴 **先看这一条**：令牌在**你的个人账号设置**里，不在**仓库设置**里。
> - 对的地址栏：`github.com/settings/...`
> - 错的地址栏：`github.com/<你的账号>/<仓库>/settings/...`（这是仓库设置，里面没有 Developer settings）
> 另外 `Developer settings` 在左侧栏的**最底部**，需要把左栏拉到底才能看到。

**最快的方式：直接开这个链接**（跳过所有导航）

- 新建细粒度令牌：<https://github.com/settings/personal-access-tokens/new>
- 令牌列表：<https://github.com/settings/personal-access-tokens>
- 经典令牌（备用）：<https://github.com/settings/tokens>

如果链接打开正常，说明你之前只是没找对位置，按下面填写即可：

1. 点右上角头像 → **Settings**
2. 左侧栏**拉到最底部** → **Developer settings**
3. 左栏 **Personal access tokens** → **Fine-grained tokens** → **Generate new token**
4. 按下表填写：

| 字段 | 填什么 |
| --- | --- |
| Token name | 例如 `demo-site-push` |
| Expiration | 建议 90 天（到期后重新生成一次即可） |
| Repository access | 选 **Only select repositories** → 勾选刚才创建的仓库 |
| Permissions → Repository permissions → **Contents** | 设为 **Read and write** |

5. 点 **Generate token**，**立刻复制**页面上出现的 `github_pat_...` 字符串

> ⚠️ 令牌只显示这一次，关掉页面就再也看不到了。没复制到就重新生成一个。
> 权限只给了这一个仓库的 Contents，风险可控；不用了随时可以在同页面 Revoke。

**如果确实找不到 Developer settings**：你可能是组织账号、或账号受企业策略管理，个别账号会被限制。这时用路 A（浏览器登录）即可绕过，不需要令牌。

## 第 4 步：本地配置

1. 打开 `D:\北京无线局项目\demo-site` 目录
2. 双击 **`首次配置.bat`**
3. 按提示依次输入：
   - GitHub 账号名
   - 仓库名（`gd-safety-quality-demo`）
   - 提交用的姓名、邮箱（随便填，只用于 git 提交记录）
   - 令牌：走**路 B** 就粘贴第 3 步复制的令牌；走**路 A** 就直接回车跳过

看到「配置完成」即可。令牌会存进 Windows 凭据管理器，**不会明文写进仓库文件**。

## 第 5 步：首次发布（必须做在开 Pages 之前）

1. 双击 **`发布更新.bat`**
2. 输入本次更新说明（可留空），回车
3. 等它跑完，结尾会打印稳定地址

```
发布完成，稳定地址：
  https://<账号>.github.io/<仓库>/
```

跑完这一步，GitHub 仓库里就有内容了，`main` 分支也就存在了。可以回浏览器刷新仓库页面确认能看到 `index.html`、`versions` 等文件。

> 想确认分支名：`首次配置.bat` 跑完时会打印一行 `当前分支：main`。如果显示的是 `master`，第 6 步 Pages 里就选 `master` —— **脚本不挑分支名，Pages 里选哪个分支，你就推哪个分支**。

## 第 6 步：开启 GitHub Pages（现在才有 main 可选）

1. 回到该仓库页面，点 **Settings**（顶部菜单最后一个）
2. 左侧栏找到 **Pages**
3. **Source** 选择 **Deploy from a branch**
4. 此时 **Branch** 下拉里就有 **`main`** 了 → 选 `main`，目录选 **`/ (root)`**，点 **Save**
5. 等 1–2 分钟，页面顶部会提示 `Your site is live at https://<账号>.github.io/<仓库>/`

> 如果 Branch 下拉**仍然是空的**，说明第 5 步的推送还没成功，或者你看的不是刚才那个仓库。回第 5 步看有没有报错（见文末常见问题）。

## 第 7 步：验证

浏览器打开上面的地址（首次生效需 1–2 分钟，可稍等后刷新）。应该看到：

- 最新版本号（自动从演示文件读取，当前是 **V1.6**）
- 一个「打开演示」按钮 → 直接进原型
- 「下载需求说明书 V1.6」
- 下方历史版本表格

把 **首页地址** 发给研发即可长期使用，以后每次发布内容自动更新，地址不变。

---

## 常见问题

**推送报 `Invalid username or token. Password authentication is not supported for Git operations.`** ← 最常见
意思是：git 这次用**用户名+密码**去登录，而 GitHub 早已不接受账号密码。

**一行修复**：双击 **`修复登录.bat`**（它会把认证方式锁成"浏览器授权"），然后再双击 `发布更新.bat`，此时会自动打开浏览器，用 GitHub 账号点 **Authorize** 即可。

> 千万不要在密码提示里填 GitHub 登录密码——必须是令牌或浏览器授权。
> 如果浏览器授权方式不成功，改用设备码：在 `demo-site` 目录执行
> ```powershell
> .\fix-auth.ps1 -UseDeviceCode
> ```
> 再跑 `发布更新.bat`，屏幕会给一个 8 位代码，打开 <https://github.com/login/device> 输入并授权。

**找不到 Developer settings（或左侧栏没有这一项）**
两个最常见原因：
1. **位置找错了**：令牌在**个人账号设置** `github.com/settings`，不在**仓库设置** `github.com/<账号>/<仓库>/settings`。如果你是从仓库页点进去的 Settings，里面只有仓库相关配置，没有 Developer settings。
2. **没拉到最底**：它在个人设置左侧栏的**最底部**，需要滚动左栏。

最省事的做法：直接打开 <https://github.com/settings/personal-access-tokens/new>，能打开就说明只是导航没找对。
或者干脆**不用令牌**，走第 3 步的路 A（浏览器登录）。

**Pages 里 Branch 下拉是空的 / 没有 main 可选**
因为仓库还是空的，一个分支都没有。Pages 的下拉只列出已存在的分支。**先完成第 5 步推送**（`发布更新.bat` 跑完且没报错），回仓库页面刷新能看到文件了，Branch 下拉里就会出现 `main`。
如果已经推送成功还是没有：确认你看的是同一个仓库（地址栏 `github.com/<账号>/<仓库>`）；再确认推送时打印的分支名是 `main` 而不是 `master`，是 `master` 就在下拉里选 `master`。

**推送时弹出登录窗口，选哪个**
选 **Browser / 浏览器**，然后在打开的网页里点 Authorize。这是 Git Credential Manager 的正常流程，不需要令牌。

**推送时提示 `schannel: AcquireCredentialsHandle failed`**
本机 git 默认 TLS 后端有问题。脚本已自动改用 openssl，若仍报错，在 `demo-site` 目录执行一次：
```powershell
git config --local http.sslBackend openssl
```

**推送时提示 `Repository not found`**
仓库名或账号名拼错了。重新运行 `首次配置.bat` 覆盖即可。

**推送时提示连接被重置（Connection was reset）**
出口网络瞬时抖动，直接再双击一次 `发布更新.bat` 重试。

**Pages 打开是 404**
依次检查：仓库 Settings → Pages 里 Branch 是否选了 `main` 且目录是 `/ (root)`；仓库根目录下是否存在 `index.html`（看仓库文件列表）；刚推送时等 1–2 分钟。

**中文文件名会不会有问题**
发布站点里所有路径都是英文（`versions/2026-09-17-v1.6/`），只有需求文档的下载文件名是中文，浏览器下载正常。原始中文文件名的 `0917优化项.html` 只保留在工作区，不进站点。

**不想装脚本，能不能只手动上传？**
可以。在仓库页面点 **Add file → Upload files**，把 `demo-site` 里的 `index.html`、`latest.html`、`versions` 文件夹整个拖进去，填一句提交说明即可。缺点是每次都要手动拖，而且容易漏文件，建议只用这一次。

---

## 合规提醒（请务必看一眼）

选择的方案是**公开仓库**，这意味着：

- 任何拿到地址的人都能访问，内容会被搜索引擎收录，`github.com/<账号>/<仓库>` 上的源码也公开可见
- 当前演示文件包含：项目名称（国家广播电视总局大湾区发射台）、参建单位名称（中交四航局、深圳市建安等）、检查标准与整改业务细节

如果这些信息不适合公开，三个补救方向（按推荐度）：

1. **改为私有仓库 + Pages**：仓库源码不再公开，但 Pages 页面本身仍是"有链接即可访问"。需要 GitHub Pro/Team（私有仓库开 Pages 是付费能力）。操作：仓库 Settings → General → 最下方 Change visibility → Make private，然后在 Pages 里确认仍为 `main / root`。
2. **改用单位内部 GitLab Pages**：内容不出内网，最稳妥。本套脚本结构不变，只需把 `setup.ps1` 里的远端地址换成 GitLab 仓库地址，Pages 由 `.gitlab-ci.yml` 或仓库设置发布。
3. **发布前脱敏**：把项目名、单位名替换为泛称后再发布，保留完整功能演示。

需要其中任何一种，告诉我即可，我把脚本和站点结构一次改好。
